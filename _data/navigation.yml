header:

  - titles:
      # @start locale config
      en      : &EN       Lineage
      # @end locale config
    url: /lineage.html


  - titles:
      # @start locale config
      en      : &EN       Links
      # @end locale config
    url: /links.html

  - titles:
      # @start locale config
      en      : &EN       FAQ
      # @end locale config
    url: /faq.html

  - titles:
      # @start locale config
      en      : &EN       About Us
      # @end locale config
    url: /about.html

  - titles:
      # @start locale config
      en      : &EN       Orishas
      # @end locale config
    url: /orishas.html
